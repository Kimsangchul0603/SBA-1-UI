npm i -g yarn
yarn add global create-react-app
create-react-app uiproject
yarn start

code

public/index.html

<div id='root'></div>

id가 root인 div

App.js로 가서
function App() ...
이것은 class ... 아니고 function ...으로 되어 있다.

class... => OOP
function ... => functional Programming => FP

OOP(객체지향 프로그래밍) VS FP(함수형 프로그래밍)

App.js로 들어갔더니 FP로 되어있다.

람다함수(Lamda Function)
 - 익명 함수(Anonymous functions)

 ['name'] variable {'name':'hong'} value
 ->, =>
 thin arrow(java), fat arrow(js)

 javascript 언어에서 데이터타입

 두가지 변수 let, 상수 const

 위키: SPA
    싱글 페이지 애플리케이션(single-page application, SPA, 스파)은
    서버로부터 완전한 새로운 페이지를 불러오지 않고 현재의 페이지를
    동적으로 다시 작성함으로써 사용자와 소통하는 웹 애플리케이션이나 웹사이트를 말한다.

    page = html, 즉 html파일이 하나있다.

    교과서 페이지 수? 644page..
    static 상태라고 한다...
    글자가 인쇄가 되서 내용을 바꿀수가 없다...

    ebook을 샀을 때
    1 page입니다.
    
    jsx, xls, xlsx (x=extand, 확장팩)


    클릭 -> event

    {} Javascript object Notaion 자바스크립트 객체 표기법 JSON
    [] array [{}, {}, ...]

react js에서 Hook을 이용하여 코딩